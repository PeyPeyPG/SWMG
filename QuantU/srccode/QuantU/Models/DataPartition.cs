namespace QuantU.Models;

public class DataPartition {
    
    public JSON pullInfo(API){
        JSON_Data = pullAPIData(API.url);
        return JSON_Data;
    }

    public DataPartition(JSON data) {
        return partitionedData;
    }

    public StructuredFormat structureFormat(userInput) {
        return structuredFormat;
    }

    public OutputFormatting outputFormat(DecryptData) {
        return outputFormatting;
    }

    public  
}